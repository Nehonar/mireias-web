Coloca aquí las vistas de página completas (ej. `home`, `about`, `contact`). Usa componentes de `../components` y estilos globales para mantener DRY. Cada página debería:
- Importar `../styles/global.css` o bundler equivalente.
- Mantener la lógica de datos desacoplada (llamadas a servicios en `../utils`).
- Exponer solo la UI; validaciones y efectos deberían vivir en helpers reutilizables.
